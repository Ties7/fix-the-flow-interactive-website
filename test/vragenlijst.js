let teller = 0

function updateUI() {
    // - Pak huidige actieve section en maak die onzichtbaar
    // - Pak section op positie aangegeven door teller en maak die zichtbaar
    // - (Hoog progress bar op)
    // - (Voeg on-intro klasse toe aan footer als de teller op 0 staat)
}

function volgendeStap() {
    // - Tel 1 bij teller op
    // - Voer updateUI() functie uit
}

function vorigeStap() {
    // - Trek 1 van teller af
    // - Voer updateUI() functie uit
}

// - Als je op knoppen met 'go-back' klasse klikt: voer vorigeStap() uit
// - Als je op knoppen met 'go-forward' klasse klikt: voer volgendeStap() uit
// (Dit zou je kunnen doen door een lijst met die klasses te queryen)

// - Voer updateUI() uit om UI te initialiseren